import React, { Component } from 'react';
import PropTypes from 'prop-types';


const Loader = (props) =>
  <img className="loader" src="/assets/bitcoin-loader.png" alt="loading..." />;

export default Loader;
